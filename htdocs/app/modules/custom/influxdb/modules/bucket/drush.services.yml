services:
  influxdb_bucket.commands.query:
    class: Drupal\influxdb_bucket\Commands\QueryCommand
    arguments:
      - '@influxdb.services.client_factory'
    tags:
      - { name: console.command }
