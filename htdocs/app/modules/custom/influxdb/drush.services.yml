services:
  influxdb.commands.query:
    class: Drupal\influxdb\Commands\QueryCommand
    arguments:
      - '@influxdb.services.client_factory'
    tags:
      - { name: console.command }

  influxdb.commands.recommend:
    class: Drupal\influxdb\Commands\RecommendCommand
    tags:
      - { name: console.command }
