operations:
  getHelp:
    httpMethod: 'GET'
    uri: /manageAPI/getHelp
    summary: Retrieve API version and supported endpoints.
    responseModel: GetHelp

models:
  GetHelp:
    type: object
    location: json
    properties:
      APIVersion:
        location: json
        type: string
      SupportedEndpoints:
        location: json
        items:
          type: string
